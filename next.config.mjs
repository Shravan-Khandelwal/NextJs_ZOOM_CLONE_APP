/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'],  // Add Unsplash or other external domains here
      },
};

export default nextConfig;
